const fs = require("fs");

const getNotes = function () {
  return "Your Notes...";
};

const addNotes = function (title, body) {};

const loadNotes = function () {};

module.exports = {
  getNotes: getNotes,
  addNote: addNotes,
};
